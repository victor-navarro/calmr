#' Get options for heidi model
#' @param iterations An integer denoting the number of iterations to run
#' @examples
#' getHeidiOpts(iterations = 10)
#' @export
getHeidiOpts <- function(iterations = 1){
  return(list(iterations = iterations))
}
